# ── SECRETS & CREDENTIALS (never commit these) ─────────────────────────
cookies.txt
music.apple.com_cookies.txt
*.cookies.txt
wrapper-v2/.env

# ── The wrapper: cloned repo, extracted Android libs, cached Apple session ──
wrapper-v2/
wol-wrapper/
wrapper-v2/data/
# amdl wrapper — Apple session DBs + login.env (Apple ID credentials) live here
wrapper-amdl/

# ── ffprobe quality cache: absolute local paths, machine-specific ─────────
quality_cache.json

# ── Android APKs (large binaries) ──────────────────────────────────────
*.apk

# ── Release artifacts (built locally for the GitHub release) ────────────
dist/
# ── PyInstaller build droppings (from local binary builds) ───────────────
build/
*.spec
*.apkm
*.apks

# ── Python ─────────────────────────────────────────────────────────────
.venv/
__pycache__/
*.pyc
.pytest_cache/

# ── Local state / logs ─────────────────────────────────────────────────
data/
*.log
.DS_Store

# ── Generated app bundle (rebuild with ./make_app.sh) ─────────────────
Music High Res.app/

# ── Editor / OS ────────────────────────────────────────────────────────
.vscode/
.idea/

# ── gamdl scratch (encrypted temp downloads) ────────────────────
gamdl_temp_*/
